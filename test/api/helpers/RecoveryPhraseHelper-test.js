import RecoveryPhraseHelper from '../../../src/api/helpers/RecoveryPhraseHelper'

describe('...testing RecoveryPhraseHelper', async () => {
  it('Recover a user...', async () => {
    // const user = RecoveryPhraseHelper.recoverUser(
    //   'crouch loan escape idea drop blush silver history gentle pave office ginger'
    // )
    // console.log(JSON.stringify(user, null, 2))
  })
})
